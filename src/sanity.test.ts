import { describe, expect, it } from 'vitest'

// Placeholder smoke test so the CI test gate is wired and green.
// Replace with real component/permission tests as modules are built (CLAUDE.md §11).
describe('build sanity', () => {
  it('runs the test runner', () => {
    expect(1 + 1).toBe(2)
  })
})
